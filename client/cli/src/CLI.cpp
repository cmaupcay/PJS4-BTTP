#include "../include/CLI.h"

namespace BTTP
{
    namespace Client
    {
        namespace CLI
        {
            const int executer(const int& argc, const char** argv)
            {
                bool actif = true;
                int retour = 0;
                std::string tampon, commande = "";

                while (actif)
                {
                    std::cin >> tampon;
                }

                return retour;
            }
        }
    }
}