import 'package:flutter/painting.dart';

class ThemeColor {
  Color fond;
  Color bar;
  Color button;
  Color textColor;

  ThemeColor({
    required this.fond,
    required this.bar,
    required this.button,
    required this.textColor,
  });
}
